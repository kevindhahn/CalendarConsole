using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace CalendarTests
{
    [TestClass]
    public class UTCalendar
    {
/*
        [TestMethod]
        public void SixWeekMiddle()
        {
            DateTime value = new DateTime(2020,5,12);
            int expected = 6;

            Calendar.MyMonth cal = new Calendar.MyMonth(value);
            int result = cal.NumberOfWeeks(DayOfWeek.Sunday);

            Assert.AreEqual(result, expected, "5 month week Failed");
        }

        [TestMethod]
        public void SixWeekSaturday()
        {
            DateTime value = new DateTime(2020, 5, 23);
            int expected = 6;

            Calendar.MyMonth cal = new Calendar.MyMonth(value);
            int result = cal.NumberOfWeeks(DayOfWeek.Sunday);

            Assert.AreEqual(result, expected, "5 month week Failed");
        }

        [TestMethod]
        public void SixWeekSunday()
        {
            DateTime value = new DateTime(2020, 5, 24);
            int expected = 6;

            Calendar.MyMonth cal = new Calendar.MyMonth(value);
            int result = cal.NumberOfWeeks(DayOfWeek.Sunday);

            Assert.AreEqual(result, expected, "5 month week Failed");
        }

        [TestMethod]
        public void SixWeekMonday()
        {
            DateTime value = new DateTime(2020, 5, 25);
            int expected = 6;

            Calendar.MyMonth cal = new Calendar.MyMonth(value);
            int result = cal.NumberOfWeeks(DayOfWeek.Sunday);

            Assert.AreEqual(result, expected, "5 month week Failed");
        }
*/
    }
}
